//- 구독 버튼은 Author.pug에서 직접 렌더링됨
//- 이 파일은 티스토리 기본 구독 버튼 스타일만 관리

style(once='content-subscription-button')
  :postcss
    /* 티스토리 기본 구독 버튼 숨김 */
    .container_postbtn > .btn_subscription:not([data-teleport-target]) {
      @apply hidden;
    }
